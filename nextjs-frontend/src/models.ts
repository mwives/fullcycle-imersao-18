export type EventModel = {
  id: string;
  name: string;
  organization: string;
  date: string;
  price: number;
  rating: string;
  imageUrl: string;
  location: string;
};

export type SpotModel = {
  id: string;
  name: string;
  status: string;
};
